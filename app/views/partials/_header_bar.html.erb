<nav class="nav-bar-global">
  <div class="nav-bar-header">
    <h1> <%= link_to(current_user.username, current_user) %> </h1>
    <% if check_if_faculty %>
      <h1 id="new-prop-head"><%= link_to("New Proposal", new_proposal_path) %></h1>
    <% end %>
  </div>

  <div class="nav-bar-buttons">
    <ul class="nav-options-list">
      <li class="list-item"><%= link_to("Categories", categories_path) %></a></li>
      <li class="list-item"><%= button_to("Log out", :sessions, method: :delete, class: "logout") %></li>
    </ul>
  </div>
</nav>
